export type Registration = {
    id: number,
    title: string,
    dateSubmitted: string,
    status: string
};

export type UserProfile = {
  firstName: string;
  lastName: string;
  emailAddress: string;
}